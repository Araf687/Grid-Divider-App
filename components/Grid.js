import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default Grid=({height,width,bg})=> {

    return (
      <View style={{height:height,width:width,borderWidth:1,borderColor: 'black',backgroundColor:bg,}}>
       
      </View>
    )
}
